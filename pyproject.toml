[project]
name = "benchmark-polars-duckdb"
version = "0.1.0"
description = "Benchmark de conversão de CSV para Parquet com DuckDB e Polars"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.5.1",
    "polars>=1.39.3",
]
